spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/glaceon_gatewayglaceon_gateway}
    #    url: jdbc:postgresql://localhost:5432/crm_test
    username: postgres
    password: 98836
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
server:
  port: 8081